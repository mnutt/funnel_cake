		var period = $('date_period').value;
		var landing_page_match = $('landing_page_match').value;
		var referer_match = $('referer_match').value;
		var visited_page_match = $('visited_page_match').value;
		var opts = {
			filter_data: $H({
				first_event_with: { url: landing_page_match, referer: referer_match },
				has_event_with: { url: visited_page_match }
			}).toJSON()
		};
		opts.time_period = periodDays(period);
		opts.period_start = periodStart(period);
		opts.period_end = periodEnd(period);
